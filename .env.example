# Django
DEBUG=True
SECRET_KEY=INSECURE-change-me-in-production
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,[::1]

# Data directory - where sqlite db, cache, and other persistent data live
# Default: ./.data (relative to project root)
DATA_DIR=./.data
